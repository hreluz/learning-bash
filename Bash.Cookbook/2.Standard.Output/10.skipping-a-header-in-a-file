#!/bin/bash

echo $'\nOn tail, skipping the first line can be achieved as: tail -n +2 filename'
echo $'\nSpecifying a number starting with a plus sign (+) indicated an offset relative to the top of the file'
echo $'\n\t tail -n +1 -> will give the entire file'
echo $'\n\t tail -n +2 -> will skip the first line'
