#!/bin/bash

# Promp the user for a password, but not echoed on the screen
# -s is for silent

read -s -p "password: " PASSWD
printf "%b" "\n"

read -p "hello: " hello

# NOTE: read a password into an environment varialbe it is in memory in plain text, and thus may be accesed via a core dump or /proc/core
#read -s -p "password: " PASSWD; print "%b" "\n"
#echo $PASSWD

